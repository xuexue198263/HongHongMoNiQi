# 哄哄模拟器 (Honghong Simulator)

## 项目概览
哄哄模拟器是一个娱乐型网页应用，用户在各种恋爱吵架场景中练习"哄人"，通过选择选项或自定义输入来降低恋爱对象的愤怒值。

## 版本技术栈
- **Framework**: Next.js 16 (App Router)
- **Core**: React 19
- **Language**: TypeScript 5
- **UI 组件**: shadcn/ui (基于 Radix UI)
- **Styling**: Tailwind CSS 4
- **AI SDK**: coze-coding-dev-sdk

## 目录结构
```
├── src/
│   ├── app/
│   │   ├── page.tsx          # 首页 - 游戏设置（选择性别、地区、姓名、头像）
│   │   ├── game/page.tsx     # 游戏主页面 - 聊天交互
│   │   ├── result/page.tsx   # 结果页面 - 成功/失败/分享
│   │   ├── layout.tsx        # 全局布局
│   │   ├── globals.css       # 全局样式（含自定义动画）
│   │   └── api/game/
│   │       ├── avatars/route.ts  # 生成4种类型头像
│   │       ├── start/route.ts    # 初始化游戏（生成场景+初始对话）
│   │       ├── chat/route.ts     # 处理每轮对话（AI判定+愤怒值+语音）
│   │       └── tts/route.ts      # 单独的TTS接口
│   ├── components/ui/        # Shadcn UI 组件库
│   ├── lib/
│   │   ├── game-types.ts     # 类型定义、常量、头像/语音映射
│   │   ├── game-prompts.ts   # AI prompt 模板
│   │   └── utils.ts          # 通用工具函数
│   └── server.ts             # 自定义服务端入口
├── DESIGN.md                 # 设计规范
└── AGENTS.md                 # 本文件
```

## 构建与测试命令
- 开发: `pnpm dev`
- 构建: `pnpm build`
- 类型检查: `pnpm ts-check`
- Lint: `pnpm lint`
- 启动生产: `pnpm start`

## 代码风格指南
- 严格 TypeScript，禁止隐式 any
- 函数参数和返回值必须有类型标注
- 全中文 UI，禁止英文文案
- 配色以珊瑚粉(#FF6B6B)为主色，暖奶油白(#FFF8F0)为背景
- 所有 AI 能力通过 coze-coding-dev-sdk 在后端调用

## 核心游戏逻辑
1. 首页排列顺序：恋爱对象姓名 → 恋爱对象性别（女朋友/男朋友）→ 方言地区（含粤语/四川话/闽南语/上海话/东北话/湖南话/山东话/河南话/陕西话/江西话/普通话）→ 选择吵架场景 → 生成4种类型头像 → 选择头像 → 开始游戏
2. 10个预设吵架场景展示在首页（忘了纪念日/忘了生日/约会迟到/异性误会/手机隐私/拿别人比较/消息已读不回/前任相关/食言而肥/家庭聚会上失言），AI基于选中场景丰富细节
3. 恋爱对象发起对话 → 用户选择/输入 → AI 判定
4. 愤怒值 0-100，4档等级（暴怒/不爽/心软/原谅）
5. 紧急挽救机制：首次超过100回落到100
6. 10轮内降到0为胜，评价分4个等级
7. 选项为纯口头回复内容，不加动作描述/旁白/表情符号
8. 场景描述后不给出行动建议，只给出3-5个隐藏关键词提示（hints），关键词不在场景中出现，是后续对话中能起安抚作用的线索
9. 用户在对话中提到关键词可额外多降5-10分愤怒值
8. 自定义输入分值范围0-50，预设选项分值范围0-20
9. 游戏结束时恋爱对象说一句结束语（AI生成+语音）
10. 聊天界面参考微信聊天界面风格
11. 成功减分时播放成功音效+成功动画，失败加分时播放失败音效+失败动画
12. 结果页支持保存成绩单（含二维码，可扫码直接体验）

## AI 模型使用
- LLM: `doubao-seed-2-0-pro-260215`（场景生成、对话判定）
- TTS: 按头像类型匹配音色，4档愤怒等级调整语速音量
- 图片生成: `doubao-seedream-5-0-260128`（头像生成）

## 关键约束
- 所有 AI SDK 调用必须传入 `HeaderUtils.extractForwardHeaders(request.headers)`
- 游戏状态存储在客户端，通过 API 请求传递
- 语音音色与头像类型绑定，全流程保持一致
- gender 字段表示恋爱对象的性别（非玩家性别）
- 首页性别选择展示为"女朋友/男朋友"
